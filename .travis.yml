language: node_js

node_js:
  - '0.10'

addons:
  sauce_connect: true

before_install:
      - "npm install -g grunt-cli"
      - "npm install -g nodemon"

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3
  - "nodemon"

script:
  - "grunt test"

